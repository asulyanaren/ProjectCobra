#include <iostream>
#include <string>
 using namespace std;

 int main() {
      // Gtnel toxum amenashat krknvox simvol@
	 char str[] = "Aren Asulyyan";
	  
	  int size = strlen(str);
	  cout<< size << endl;
	  char c;
	  char d;
	  int count = 0;
	  int z = 0;

	  for (int i = 0; i < size-1; i++) {
		  for (int j = i+1; j < size - 1; j++) {
			  if (str[i] == str[j]) {
				    count++;
				    c = str[i];
			       }
			   }
		      if(count > z){
				  z = count;
				  d = str[i];

                }
	   }

	  std::cout << z;
	return 0;
 }
